version: '3.8'
services:
  frontend:
    build: ./dashboard
    environment:
      - PUBLIC_IP=${PUBLIC_IP}
    ports:
      - "8080:80"
    depends_on:
      - backend
  backend:
    build: ./backend
    environment:
      - PUBLIC_IP=${PUBLIC_IP}
    ports:
      - "3000:3000"
