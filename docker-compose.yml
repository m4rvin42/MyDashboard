services:
  frontend:
    image: ghcr.io/m4rvin42/dashboard:latest
    environment:
      - PUBLIC_IP=192.168.2.35
 #    - PUBLIC_IP=${PUBLIC_IP}
    ports:
      - "8080:80"
    depends_on:
      - backend
  backend:
    image: ghcr.io/m4rvin42/backend:latest
    environment:
      - NODE_OPTIONS=--dns-result-order=ipv4first
      - PUBLIC_IP=192.168.2.35
 #    - PUBLIC_IP=${PUBLIC_IP}
    dns: 8.8.8.8
    ports:
      - "3000:3000"
